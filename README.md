# web-crawler
tried to make tha web crawla

# find inside element
bio_url = quote.find_element(By.XPATH, './/a[contains(text(), "about")]')

# Классы указывать в кавычках!!!
line_of_business_name
line.find_element(By.XPATH, './/div[@class="sl-content-name"]')